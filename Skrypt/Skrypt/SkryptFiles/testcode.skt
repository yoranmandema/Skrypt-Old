using System

fn DecimalToBinary (n) {
	str = ""
	rem = n

	print(rem)
		
	while (rem >= 1) {
		d = Math.Floor(rem % 2)
		rem = rem / 2
		str = str + d
	}
	
	return str.Reverse()
}

print(DecimalToBinary(8))